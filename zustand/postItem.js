// import { create } from "zustand";
// import { persist } from "zustand/middleware";

// export const useUIStore = create(
//   (set) => ({
//     isProductOpen: false,
//     toggleProductPage: () =>
//       set((state) => ({ isProductOpen: !state.isProductOpen })),
//   }),
//   {
//     name: "ui-store", // 로컬스토리지에 저장될 키 이름
//     getStorage: () => sessionStorage, // 로컬스토리지에 저장하도록 설정
//   }
// );
